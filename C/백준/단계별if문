//1330

#include <stdio.h>

int main(void) {
    int a, b;
    scanf("%d %d", &a, &b);
    if (a > b)
        printf(">");
    if (a < b)
        printf("<");
    if (a == b)
         printf("==");
    return 0;
    }
    
    
//9488


#include <stdio.h>

int main(void)
{
     int num;
    scanf("%d",&num);
    if (num>=90)
        printf("A");
    else if(num>=80)
        printf("B");
    else if(num>=70)
        printf("C");
    else if(num>=60)
        printf("D");
    else
        printf("F");
    return 0;
             
}

//2753

int main(void) {
    int num;
    scanf("%d", &num);

    if ((num % 4 == 0 && num % 100 != 0) || num % 400 == 0)
        printf("%d", 1);
    else
        printf("%d", 0);
    return 0;

}

//14681

#include <stdio.h>

int main(void)
{
    int num1,num2;
    scanf("%d %d",&num1,&num2);
    if(num1>0&&num2>0)
        printf("1");
    else if(num1>0&&num2<0)
        printf("4");
    else if(num1<0&&num2>0)
        printf("2");
    else if(num1<0&&num2<0)
        printf("3");
    return 0;
    
}

##2884번
#include <stdio.h>

int main(void){
    int a,b;
    scanf("%d %d",&a,&b);
    
    if (a==0&& b<45)
        printf("%d %d",23,60+(b-45));
    else if(b<45)
        printf("%d %d",a-1,60+(b-45));
    else
        printf("%d %d",a,b-45);
    return 0;
}
